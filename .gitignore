# Archivos de entorno y secrets — NUNCA al repo
.env
.env.local
.env.production
.env.*
*.secret
secrets/

# Backups generados durante desarrollo
js/*.bak
js/*.backup
*.bak

# Archivos de sistema
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*

# Dependencias si algún día se usan
node_modules/
.npm/

# Archivos de IDE
.vscode/settings.json
.idea/
*.swp
*.swo

# Archivos de reglas que no deben ser públicos
# (firestore.rules se despliega via Firebase CLI, no via Vercel)
# Comentar si quieren versionarlo en git:
# firestore.rules
